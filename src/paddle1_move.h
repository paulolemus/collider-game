#ifndef COLLIDER_PADDLE1_MOVE_H
#define COLLIDER_PADDLE1_MOVE_H

int paddle1_move(
    float delta,
    const Paddle p1,
    const Paddle p2,
    const Ball ball
) {
    return -1;
}

#endif // COLLIDER_PADDLE1_MOVE_H
